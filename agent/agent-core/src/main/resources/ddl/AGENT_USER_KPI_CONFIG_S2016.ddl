CREATE TABLE `AGENT_USER_KPI_CONFIG_S2016` (
  `ID`                     BIGINT(20)   NOT NULL AUTO_INCREMENT,
  `USER_ID`                BIGINT(20)   NOT NULL,
  `USER_NAME`              VARCHAR(50)  NULL     DEFAULT NULL,
  `USER_ROLE`              VARCHAR(20)  NOT NULL,
  `SALARY_START_DATE`      DATETIME     NOT NULL,
  `SALARY_END_DATE`        DATETIME     NOT NULL,
  `REGION_CODE`            INT(11)      NOT NULL,
  `REGION_NAME`            VARCHAR(200) NULL     DEFAULT NULL,
  `SETTLEMENT_TYPE`        VARCHAR(20)  NOT NULL,
  `NEW_AUTH_TARGET`        INT(11)      NOT NULL,
  `MAR_SL_TARGET`          INT(11)      NOT NULL,
  `APR_SL_TARGET`          INT(11)      NOT NULL,
  `MAY_SL_TARGET`          INT(11)      NOT NULL,
  `JUN_SL_TARGET`          INT(11)      NOT NULL,
  `SL_DSA_TARGET`          INT(11)      NOT NULL,
  `AUTH_GRADE_MATH_TARGET` INT(11)      NOT NULL,
  `NOTE`                   VARCHAR(200) NULL     DEFAULT NULL,
  `DISABLED`               BIT(1)       NOT NULL DEFAULT b'0',
  `CREATE_DATETIME`        DATETIME     NOT NULL,
  `UPDATE_DATETIME`        DATETIME     NOT NULL,
  `USER_CPA_FACTOR`        SMALLINT(6)  NULL     DEFAULT '100',
  `MARKET_STU_LEVEL`       VARCHAR(50)  NULL     DEFAULT NULL,
  PRIMARY KEY (`ID`)
)
  ENGINE = InnoDB;