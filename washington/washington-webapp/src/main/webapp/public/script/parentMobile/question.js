/* global define : true, PM : true, $:true */
/**
 *  @date 2015/11/5
 *  @auto liluwei
 *  @description 该模块主要负责 问题页面
 */


define([], function(){

    'use strict';

    var WIN = window;
/*
    $("#mockBody").bind("bodyIsShow", function(){

		var hash = $.trim(PM.questionHash);

        if($('.parentMobilePersonQuestionDetail').length > 0 && hash){
            // 虽然这种方式，已经可以兼容各个平台。 但是在webview下还是会出现问题。 留作技术债来考察。 为什么在正常浏览器上一点事也没有，在webview会出现短暂的留白
            //location.hash || (location.hash = encodeURIComponent(PM.questionHash) );

            $(window).scrollTop(
                $('#' + hash).offset().top
            );
        }

    });*/

});

